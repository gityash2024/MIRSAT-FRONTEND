{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static",
        "config": { "dist": "dist" }
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/index.html" }
    ]
  }